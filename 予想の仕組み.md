# 予想の仕組みと、どこまで検証したか

最終更新 2026-08-31

このファイルは「いま何をどう予想していて、何を確かめ、何が分からないままか」を1枚にまとめたもの。
**数字を聞かれたらここのコマンドを実行して答える。記憶から答えない。**

---

## 0. いちばん大事なこと（先に読む）

### 鉄則：確定オッズで出した数字は、運用の根拠にしない

締切後に決まるオッズを使って出した回収率は、**測るときの上限**であって実行できる数字ではない。
運用に載せてよいのは「締切前に手に入る情報だけ」で出した数字だけ。

この誤りは**3回**やった。3回目は、一度「実行不能」と結論したあとに再発している。

| | 確定オッズでの主張 | 締切前での実測 |
|---|---|---|
| オッズ2.5〜3.5倍を狙う | 146% | 64.2% |
| 単勝 余裕1.3・全6艇・校正あり | 172.7% | **87.1%** |
| 配信の絞り（市場と一致） | 3連単4点37.67% | 朝には判定できない |

**回収率を名乗るときは `scripts/claim.mjs` を通す。**
締切前と確定の両方を必ず出し、標本が足りなければ「まだ言えない」と返す。
片方だけを画面や結論に載せない。

---

**単勝だけが勝てる。3連単・3連複は勝てない。** 理由まで分かっている。

| | モデル | 市場 | 差 |
|---|---|---|---|
| 1着を当てる | **57.35%** | 55.24%（単勝オッズ由来） | **モデルが+2.11pt** |
| 1着を当てる | 57.35% | **58.21%（3連単オッズ由来）** | 市場が+0.86pt |
| 2着（1着を知った前提） | 42.15% | **43.18%** | 市場が+1.03pt |
| 3着（1着2着を知った前提） | 41.77% | **42.79%** | 市場が+1.02pt |

**同じ市場でも3連単オッズのほうが単勝オッズより3pt正確。** 売上規模が桁違いで、読みがそこに集まるため。
だから**単勝の市場は甘く、そこだけ突ける**。3連単の市場は鋭いので勝てない。

---

## 1. データの流れ

```
公式サイト
  ↓ download.mjs / extract.mjs   番組表(B)・競走成績(K)
  ↓ build.mjs                    races / entries / programs
  ↓ odds.mjs odds2.mjs odds3.mjs 確定オッズ（単勝複勝／2連／3連）
  ↓ before.mjs                   直前情報（展示タイム・チルト・展示進入・展示ST・調整重量・気温水温）
  ↓ derive.mjs                   feat  艇単位194項目（時点を守って積み上げ）
  ↓ racefeat.mjs                 rfeat レース単位48項目
  ↓ walk.mjs                     毎月学習し直して翌月を予想 → wi1(1着) / wi3(3連単)
  ↓ predict.mjs                  これから走るレースを予想
  ↓ auto-bet.mjs                 締切3〜2分前に単勝を判定 → bets
  ↓ record3.mjs                  3連単・3連複・2連単を記録 → pred_daily
```

### 毎日の自動処理
| 時刻 | タスク | 中身 |
|---|---|---|
| 05:00 | `boatrace-night` → `night.sh` | 前日の結果・確定オッズ・直前情報を収集、予想に当たり外れを埋める、成績レポート |
| 06:00 | `boatrace-morning` → `morning.sh` | 当日の番組表→特徴量→**全レース予想**→3連単等を記録、auto-bet起動 |

⚠ `predict.mjs` は **`--trio` を付けないとJSONを出さない**（出力が --trio の枝の中にある）。
外すと `record3.mjs` が何も記録できない。

---

## 2. 何を予想しているか

各艇の点数 = 項目 × 係数 を全部足したもの。**線形1層**。
1着用・2着用・3着用の3組の係数を持ち、順に選ぶ（Plackett-Luce）。

### 時点を守る（ここが崩れると全部無意味）
`derive.mjs` は日付順に流し、**結果を数える前に特徴量を書く**。
場ごとの傾向（`vn_*` `vh_*`）も同じく、そのレースより前の実績だけで計算する。

### 使わないもの（締切後にしか分からない）
- 進入コース（`entries.course`）… 枠番を使う。一致は約85%
- レース時の風速・波高（`--nowind`）… Kファイルの記録値で締切前には無い

### 項目 224（walk.mjs）
| 種類 | 数 | 中身 |
|---|---|---|
| 艇単位 `feat` | 150 | 選手のコース別42・当地14・級別戦7・直近10走/30走10・決まり手14・モーター18・ボート7 ほか |
| レース単位 `rfeat` | 48 | 相対順位・拮抗度・前づけ危険度・場×時間帯・展示タイム・展示進入・展示ST・気温水温・調整重量・身長 |
| 番組表 | 9 | 年齢・体重・勝率・当地勝率・モーター2連率・ボート2連率・早見 |
| 枠ダミー | 6 | |
| 条件補正 `adj_*` | 11 | 場・グレード・レース番号・時間帯・日目・節の長さ・残り日数・格・**風向**・**選手級別**・**支部** をコースと掛けて実績から算出（縮小推定つき） |

---

## 3. 買い方（単勝）

**必要倍率 =（1 ÷ モデルの確率）× 余裕　オッズがそれ以上なら買う。**

- 選ぶのは**モデルの確率だけ**。オッズは「買うか見送るか」の比較にしか使わない → 実行可能
- **本命1艇に限らず全6艇が対象**。1レースで複数買うことも、1本も買わないこともある
- 確率は**校正して使う**（確率帯 × オッズ帯の升目で、学習期間の実績に引き直す）

### 実測（45,273レース・2025-11〜2026-08・**実際の払戻ベース**）
| 余裕 | 1日 | 的中率 | 平均払戻 | 回収率 | 1日100円買いの利益 |
|---|---|---|---|---|---|
| 1.0 | 151.7本 | 21.24% | 607円 | 128.89% | +4,382円 |
| 1.2 | 82.8本 | 16.99% | 903円 | 153.31% | **+4,415円** |
| 1.3 | 64.4本 | 15.74% | 1,052円 | **165.58%** | +4,221円 |
| 1.5 | 42.1本 | 14.25% | 1,315円 | 187.35% | +3,679円 |
| 1.8 | 25.0本 | 12.55% | 1,727円 | 216.84% | +2,924円 |

**9ヶ月すべて100%超。** 校正なしだと余裕1.3で141.95%なので、校正で+23.6pt。

⚠ **オッズ×100 で計算すると実際の払戻より1.6〜3.2pt高く出る**。必ず `payouts` で検算する。

---

## 4. 分かっていること

### 有効だったもの
- **必要倍率の判定**（単勝）… 上記
- **確率の校正**（確率帯×オッズ帯）… 余裕1.3で141.95%→165.58%
- **全6艇を対象にする**… 本命1艇だけより1日の利益が5倍
- **rfeat 48項目**… 1着 56.60%→57.35%、2連単4点 58.93%→60.01%
- **展示の進入コース**… 前づけレースの49.21%で6艇一致（枠なり前提は0%）

### 効かなかったもの（測って外した）
| 手 | 結果 |
|---|---|
| 案D（コース別の重み） | 1着 −4.5pt |
| 共通＋枠ごとの差分 | −1.0pt |
| 着順パターン120通りを一度に比べる | −1〜3pt |
| 勾配ブースティング（1着） | +0.19pt（回収率では線形が上） |
| 勾配ブースティング（3段階） | 2連単 −0.41pt |
| 2着を「誰が1着か」で場合分け | ±0pt |
| モンテカルロ（正規・裾の重い分布） | −0.07〜0.10pt |
| 割引つきHarville | 順位が変わらないので効果なし |
| 市場を混ぜる | +0.21pt |
| 本番の進入コースを知る（上限） | +0.29pt |
| べき乗法・Shin法でオッズ→確率 | 単純正規化が最良 |
| Harville推定で3連単オッズを作る | 中央33%ずれ・回収61〜77%に悪化 |

### 未解決
- **締切間際の値動き**… 判定時のオッズと確定オッズがずれる。締切0分前でも±10%以内は23.5%。
  本命は中央7%下がる。`bets.odds_seen` と確定オッズの突き合わせで運用しながら検証中
- **校正後も2.2〜3.4pt自信過剰**が残る
- 実オッズでの100点以上の実績はまだ無い（1点100円・100点貯まるまで判断しない）

---

## 5. 撤回した主張（同じ誤りを繰り返さないため）

1. **「オッズ2.5〜3.5倍を狙えば146%」** → 確定オッズで選んでいた。締切前オッズでは64.2%
2. **「市場の1番人気が1着＝51.83%」** → 正しくは55.24%。モデルの優位は5ptでなく2.10pt
3. **「3連単オッズは未取得」** → 取得済みだった（テーブル一覧を`head`で切って見落とし）
4. **「並び全体で比べる方式が2連単で+9.66pt」** → 学習12回の途中経過。学習量をそろえると逆転
5. **「案Dは全項目を演算に使う」** → 条件補正をダミーに置き換えて**項目を減らしていた**
6. **「1号艇が僅差で2番手なら入れ替えると+0.69pt」** → rfeatバグの埋め合わせ。修正版では+0.07pt
7. **「自信過剰なのに理屈を上回る」** → 理屈上の回収率を余裕の数字と取り違えていた。
   正しくは「p×オッズの平均」で、余裕1.3なら178.33%。実測148.88%は**理屈を下回っている**
8. **「配信は市場と一致するレースに絞る（3連単4点37.67%）」** → その市場オッズは**確定オッズ**で、
   3連単オッズは翌日にしか取っていないので**朝には判定できない**。1.と同じ型。
   さらに朝に使える自信度絞りのほうが同じ本数で**上**だった（3連複4点で+7.4pt）ので、案ごと廃棄
9. **「配信の足切りは自信度0.8282」** → 歩進検証(wi3)の分位点だった。本番モデル(pred3)では
   同じ値が上位2.8%＝1日4本になる。本番の上位10%は**0.7645**。的中率は両者ほぼ同じで、
   **ずれていたのは閾値だけ**（測る場所を間違えても数字が「それらしく」出る例）
10. **「単勝は余裕1.3・全6艇・校正ありで回収172.7%」** → **確定オッズで判定した場合の数字**。
   単勝は総取り式で払戻は締切後に決まり、締切前の表示は当てにならない。
   締切0分前の6,486件で、表示20〜50倍は確定その0.688倍、50倍超は0.395倍に崩れる。
   判定は高オッズを狙うので直撃する（買うと決めた分の中央値0.495・85.6%が下落）。
   本番の予想×締切前オッズ×確定結果がそろった9日で**回収83.4%**（確定オッズ判定なら111.0%）。
   一律の割引も帯ごとの補正も効かなかった。**`/asa` に警告を出した（2026-08-31）**
11. **「風向・級別・支部・身長を足す」** → 1着的中は誤差の範囲（−0.39pt、McNemar z=1.47）だが
   **確率の質が有意に悪化**（対応のあるt検定 t=−7.22）。判定は確率の値そのものを使うので不採用

### 繰り返した誤りの型
- **try/catch が握り潰す**… `rfeat` が2度、丸ごと空のまま学習していた（8/27・8/29）。
  → いまは空なら例外で止まる
- **配線のずれ**… `vec()` の並びが `NAMES` とずれて39項目がゼロだった
- **途中経過で結論を出す**… 学習が収束する前の数字で判断した
- **一覧を`head`で切って見落とす**… 3連単オッズが既にあるのに「無い」と報告した
- **測る場所と動く場所が違う**… 検証は歩進検証(wi1/wi3)、実際に朝動くのは model5.json(pred/pred3)。
  1着的中も対数尤度もほぼ同じ（57.41% 対 57.20%）だが**確率の鋭さが違う**ので、
  分位点で決めた閾値は移らない。**閾値・分位点は必ず pred/pred3 側で出す**
- **確定オッズを使ってしまう**… 単勝で2度、配信の絞りで1度。
  「その情報は締切前に手に入るか」を毎回確かめる。
  ⚠ 単勝の2度目がいちばん重い：2026-08-23に「実行不能」と一度結論していたのに、
  判定を作り直したあと**確定オッズで出した172.7%を根拠に運用に載せた**。
  **確定オッズで出した数字は、締切前に実行できるかを別途確かめるまで運用の根拠にしない。**

---

## 5.4 いま唯一の買い方（B2判定）

**本命がB2級かつモデルの確率40%未満のとき、その艇の単勝を1点。1点100円。**
`scripts/b2.mjs` → `b2_daily` → 画面は `/asa` の先頭。オッズを使わないので朝に確定する。

### なぜこれだけか
オッズを使わない買い方を過去298日で総当たり（券種5種・条件7軸・展示20種・選手属性19種・
買う対象14通り・点数と配分）して、唯一残った条件。

### なぜ効くか＝看板効果
全国勝率をそろえて級別だけ変えると、同じ弱さでも回収が56pt違う。

| 全国勝率0〜4.5 | 本数 | 的中率 | 回収率 | 平均オッズ |
|---|---|---|---|---|
| **B2** | 163 | 36.8% | **148.2%** | 3.87倍 |
| B1 | 1,728 | 32.0% | 92.5% | 3.18倍 |

的中はB2のほうが高いのにオッズも高い。**出走表に印刷される「B2」を世間が過剰に割り引いている。**
だから勝率で緩めても効かない。モーター最下位(84.5%)・最年長(84.3%)も効かない
＝世間はそれらを正しく織り込んでいる。

### 実力（強いほうだけ見ないこと）

| | 本数 | 的中 | 回収 | 95%範囲 |
|---|---|---|---|---|
| 歩進検証 wi1・298日 | 254 | 36.2% | 131.5% | 105.9〜149.1% |
| **本番モデル pred・147日** | **124** | **35.5%** | **125.2%** | **90.9〜158.0%** |

**本番側は100%を含む＝まだ有意ではない。**

### 本数は増やせない（緩和も並走も試して不発）
確率上限を緩めると本番モデルで 40%→125.2% / 45%→112.9% / 50%→106.0%。
条件を重ねると半減する。**1日0.84本のまま、決着まで約237日。**
賭け金を上げても検証は早まらない（回収率は金額に依らない）。

並走できる別条件を **864通り** 探した（前半5ヶ月で選び、後半5ヶ月で試す）。
前半の候補34件のうち後半を通ったのは4件、うち3件はB2の言い換え。
**偶然通る確率を実測すると6〜12%** なので、34件なら偶然でも約2.4件通る。**4件は区別がつかない。**

### 看板効果は他でも見えるが、儲かるのは級別だけ
確率40%未満のうち **F歴あり(180日以内) 99.8% / F歴なし 87.8%**。
F1・F2の印字も過剰に割り引かれているが100%には届かない。
降級・昇級の直後(76〜94%)、モーター最下位(84.5%)、最年長(84.3%)、よそ者(92.8%)は効かない。
**過剰に割り引かれる看板は級別が突出している。**

### 賭け金の上限
そのレースの単勝プールの7.05%を入れると回収が100%に落ちる。プールは全レースのオッズの
整数制約から逆算して中央19,900円（下限）、公開情報からの上限が約45万円。
**最悪の想定でも1点1,000円までは残る。**当面は100円。

---

## 5.45 展示情報でモデルを作り直しても良くならない（2026-08-31に確認）

同じ22,614レースで、展示入りのモデル(wi1)と展示なしのモデル(pred)を比べた。

| 対象 | レース | wi1(展示あり) | pred(展示なし) | 差 |
|---|---|---|---|---|
| 全部 | 22,614 | 57.18% | 57.38% | −0.20pt |
| B2が出ているレース | 8,224 | 55.20% | 55.30% | −0.10pt |
| 両モデルとも本命がB2 | 376 | 48.94% | 48.67% | +0.27pt |
| 本命の確率が40%未満 | 2,598 | 33.18% | 32.87% | +0.31pt |

差は±0.3pt以内で符号もばらばら、対数尤度も動かない。**展示を入れる作り直しは不要。**
B2判定で wi1 が 142.9%、pred が 125.2% と開いたのは精度の差ではない
（選んだレースの重なりが41%しかない・124〜136本の選択のブレ）。

**月ごとの学習し直しも効かない。** wi1は毎月学習し直し、predは2026-03-25で止まっているが、
predのほうがわずかに上（57.38% 対 57.18%）。model5.json が5ヶ月古いのは問題ではない。

⚠ 展示は締切15分前にしか出ないので、入れるなら朝の予想をやめてレースごとに計算し直す作りが要る。
**効果が無いと分かった以上、その作り替えはしない。**

---

## 5.5 配信用（当てにいく予想）— 買う判定とは別物

娯楽・配信のために、**当たることを優先した**3連複・3連単を毎朝出す。
単勝の買い判定とは狙いが逆なので、テーブル（`haishin_daily`）も画面（`/haishin`）も分けている。

| | 買う判定 | 配信用 |
|---|---|---|
| 券種 | 単勝 | 3連複・3連単 |
| 狙い | 市場が低く見ている艇 | 堅いレース |
| 的中率 | 17.6% | 3連複4点 82.6% |
| **回収率** | **191%（プラス）** | **83%（マイナス）** |
| 本数 | 1日61本 | 1日15本 |

### 絞り方
自信度 =「3連複の上位4点の確率の合計」。朝の予想だけで決まり、**オッズを使わない**。
既定は 0.7645（本番モデルの上位10%）。

学習後の22,614レース（147日）で実測：

| 絞り | 1日 | 3連複4点 | 3連単4点 | 3連複の回収 | 3連単の回収 |
|---|---|---|---|---|---|
| なし | 153.8本 | 63.70% | 30.53% | 78.7% | 80.6% |
| 上位25% | 38.5本 | 77.41% | 41.70% | 79.4% | 86.4% |
| 上位15% | 23.1本 | 80.69% | 44.25% | 81.4% | 88.1% |
| **上位10%** | **15.4本** | **82.62%** | **45.73%** | **82.6%** | **87.5%** |
| 上位5% | 7.7本 | 83.47% | 46.95% | 79.8% | 86.2% |

上位10%が的中・回収とも最良。それ以上絞っても的中はほぼ伸びず回収は落ちる。
点数を増やすと的中は上がるが回収は下がる（3連複6点で90.49%／75.7%）。

**回収率はどの絞り方でもマイナス。配信は見せるためのもので、買うものではない。**

### 的中判定は当日のうちに入る
競走成績（Kファイル）は翌日にならないと出ない。それ待ちだと当日は当たったか分からないので、
**公式の結果ページを直接見て埋める**（`raceresult.mjs` → `result_live`）。

- 15分ごとにタスク `boatrace-haishin-fill` が動く（09:00〜22:00）
- 締切を過ぎたレースだけ叩く。まだ走っていないレースは対象にしない
- 画面 `/haishin` に「結果 1-5-3 ／ 3連単的中」と当日の集計が出る
- 翌朝の night.sh が `--recheck` で競走成績と突き合わせ直す。**食い違えば警告が出る**

⚠ 当日取りの値は `payouts`/`entries` に入れない。あの2つはKファイル由来で、
点時点を守る作りの土台になっている。混ぜると「いつの情報か」が分からなくなる。

---

## 6. 再実行のコマンド

```bash
# データ更新（前日ぶん）
bash scripts/night.sh
```

```bash
# 当日の準備と全レース予想
bash scripts/morning.sh
```

```bash
# 歩進検証（毎月学習し直す）
node --max-old-space-size=8192 scripts/walk.mjs --nobf --nowind --from 2025-06-01 --first 5 --t1 wi1 --t3 wi3
```

```bash
# 回収率を名乗るときは必ずこれを通す（締切前と確定の両方が出る）
node --max-old-space-size=8192 scripts/claim.mjs --margin 1.3
node --max-old-space-size=8192 scripts/odds-live-audit.mjs   # 締切前オッズの健全性

# 単勝の回収率に直結する指標
node --max-old-space-size=6144 scripts/edge.mjs --t wi1
```

```bash
# 校正つきの判定
node --max-old-space-size=6144 scripts/calib3.mjs --t wi1
```

```bash
# 配信用（当てにいく3連複・3連単／買う判定とは別）
node scripts/haishin.mjs --date 2026-08-31          # 記録
node scripts/haishin.mjs --date 2026-08-31 --text   # 貼れる文面
node scripts/raceresult.mjs --date 2026-08-31       # 当日の着順と払戻を公式から取る
node scripts/haishin.mjs --fill --date 2026-08-31    # その日だけ照合（15分ごとの運用はこれ）
node scripts/haishin.mjs --fill --date 2026-08-31 --recheck  # 競走成績で検算し直す
node scripts/haishin.mjs --fill                     # 全期間の未照合を拾う
node scripts/haishin.mjs --report                   # たまった分の成績
node --max-old-space-size=8192 scripts/haishin-check3.mjs   # 絞りを測り直す（本番モデルで）

# 3連単・3連複の記録と成績
node scripts/record3.mjs --report
```

---

## 7. データの範囲

| テーブル | 中身 | 期間 |
|---|---|---|
| races / entries | 222,671レース | 2022-08〜2026-08 |
| feat | 艇単位194項目 | 同上 |
| rfeat | レース単位48項目 | 同上 |
| odds_tan | 確定単勝・複勝 | 202,670レース |
| odds3t / odds3f | 確定3連単・3連複 | 56,012レース（2025-08〜） |
| before_info | 直前情報 | 全222,671レース |
| odds_live | 締切前オッズ | 1,171レース（2026-08-21〜29・**収集停止**） |
| bets | 単勝の判定記録 | 2026-08-22〜 |
| pred_daily | 3連単等の予想記録 | 2026-08-29〜 |
