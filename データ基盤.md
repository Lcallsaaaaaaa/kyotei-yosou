# 過去データ基盤（1年分・自前DB）

公式が配布している **競走成績(K)・番組表(B)** のファイルを蓄積し、SQLiteで集計する仕組み。
日和が見せている確率の大半を**自前で計算できる**ようにするのが目的。

---

## 現状

| | |
|---|---|
| 期間 | 2025-08-18 〜 2026-08-17（1年）|
| races | 54,634 |
| entries | 327,804（= 54,634 × 6 で完全一致）|
| payouts | 545,825（7券種・人気つき）|
| programs | 331,128 |
| DB | `data/boatrace.db`（152MB）|
| 取得元 | `http://www1.mbrace.or.jp/od2/{K,B}/{YYYYMM}/{k,b}{YYMMDD}.lzh` |

---

## 使い方

```bash
# 1. 取得（既存ファイルは飛ばすので、中断しても再実行で続きから）
node scripts/download.mjs 2025-08-18 2026-08-17

# 2. 展開（要 7-Zip）
node scripts/extract.mjs

# 3. DBへ投入（ANALYZE まで自動）
node scripts/build.mjs

# 4. 健全性チェック（★全期間を流したら必ず通す）
node scripts/check.mjs
```

### 集計

```bash
# 場のコース別入着率・決まり手・枠なり進入率
node scripts/stats.mjs course --jcd 19

# 場の「1-X」出目確率（＝日和の逃げシミュレーション相当）
node scripts/stats.mjs deme --jcd 19

# 選手のコース別成績・逃がし2着率/3着率
node scripts/stats.mjs racer 4179

# ★レースの出目確率を推定（1号艇から順に登番6つ）
node scripts/stats.mjs race --jcd 19 --lanes 3562,5067,4179,5176,4406,5106

# 期間指定（バックテスト時は必ず --until でレース前日を切る）
node scripts/stats.mjs race --jcd 19 --lanes ... --until 2026-08-14
```

---

## 収録データ

| テーブル | 内容 |
|---|---|
| `races` | 日付・場・R・種別・距離・天候・**風向**・風速・波高・**決まり手**・締切時刻 |
| `entries` | 着順・艇番・登番・選手名・モーター・ボート・**展示タイム**・**進入コース**・ST・レースタイム |
| `payouts` | 単勝/複勝/2連単/2連複/拡連複/3連単/3連複 の払戻と**人気** |
| `programs` | **級別**・年齢・支部・体重・全国/当地の勝率2率・モーター/ボートNOと2率 |

**風向がテキストで入っている**のが大きい。日和・公式ともWeb上は画像表示で取得できず、
2026/08/16 蒲郡9Rでは「風向が取れない」ことを理由に自信度を1段下げていた。過去分はこれで解決する。

### 日和にあって、ここに無いもの

- **オリジナル展示タイム分解**（周回 / 周り足 / 直線）… 日和の独自計測。公式は展示タイム1つしか出さない
- 選手コメント / モータ貢献ポイント

→ **展示分解については日和への依存が残る。** 8/16蒲郡9Rの3着的中はこの分解が根拠だった。

---

## ★ハマりどころ（同じ事故を繰り返さないために）

### 1. ファイルは固定幅であって空白区切りではない

```
福岡  "43 60.92162 38.10"   ← ボートNO 162 がモーター2率と密着
戸田  "  8100.00 15"        ← モーター2率 100.00 がモーターNOと密着
```

空白分割で実装すると、**ボート番号が100以上の福岡・芦屋が場ごと丸ごと欠落する**。
最初の実装がこれで、3日分で365行（10%）が静かに消えていた。**必ず桁位置で切ること。**

`programs` の桁位置：lane[0:1] 登番[2:6] 名[6:10] 年齢[10:12] 支部[12:14] 体重[14:16] 級別[16:18]
全国勝率[18:23] 全国2率[23:29] 当地勝率[29:34] 当地2率[34:40]
モーターNO[40:43] モーター2率[43:49] ボートNO[49:52] ボート2率[52:58]

`entries` の桁位置：着[2:4] 艇[6:7] 登番[8:12] 名[13:21] モーター[21:25] ボート[25:29]
展示[29:35] 進入[35:39] ST[39:47] レースタイム[47:]

### 2. ANALYZE を忘れると集計が数分固まる

統計情報が無いとプランナが結合順を誤る。`build.mjs` の末尾で自動実行するようにしてある。
手で `ANALYZE` を流す場合は1.6秒程度。

### 3. 集計を中断したら node プロセスの生き残りを確認する

タイムアウトで打ち切っても node は生きたままCPUを食い続け、後続のクエリを巻き添えに遅くする。
`Get-Process node` で確認して落とすこと。

### 4. 選手実績を場で絞ると標本が枯れる

`--jcd 19` で山下和彦の1コースは**5走**しか残らない。
`race` コマンドは **ベースレート＝当地 / 選手実績＝全場** に分けてある。

### 5. 小標本は縮小推定で潰す

「3走2勝＝66.7%」をそのまま買い目にしないため、`shrink()` で場平均へ引き寄せている（K=25）。

### 6. 進入は予想時点では未確定

`race` の推定は**枠なり進入が前提**。下関の枠なり率は76.7%＝**約4回に1回は前提が崩れる**。
`course --jcd` で場ごとの枠なり率を確認してから使うこと。

---

## 検証：2026/08/15 下関12R（最初に外したレース）

8/14までのデータだけで推定（アウトオブサンプル）：

```
出目      推定確率   必要オッズ(EV=1.0)
1-3-2      7.1%       14.1倍
1-2-3      6.8%       14.6倍   ← 実際の結果
1-3-4      4.3%       23.1倍
```

- 実際の結果 **1-2-3** を確率2位に置いた（当時の私は10点すべてから1-2を外していた）
- 実際の払戻 **¥760 = 7.6倍** に対し必要オッズは14.6倍 → **EV 0.52**
- つまりこのツールなら **「買い目を間違えた」だけでなく「そもそも見送り」と判定できた**

この2点が当時の失敗そのものなので、レース選択と買い目の両方に効く。

---

## 毎日の更新

前日分を追加する（`daily.mjs` は未実装。当面は手動）：

```bash
node scripts/download.mjs 2026-08-18 2026-08-18
node scripts/extract.mjs
node scripts/build.mjs --since 2026-08-18
```
