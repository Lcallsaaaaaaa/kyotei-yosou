# 検索（SEO）とアクセス解析

2026-09-23 に土台を作成。

## 直した3つの穴

作った当初のサイトは、**検索エンジンから見ると1ページしかありませんでした**。

| 穴 | 直したこと |
|---|---|
| URLが `#/race/…`（ハッシュ）だった | **実パス `/race/…` に変えた**。ハッシュから後ろは検索エンジンにとって同じ1つのURLなので、何ページ作っても1ページ扱いになる |
| sitemap.xml も robots.txt も無かった | `sync-public.mjs` が毎回 `site/` に書き出す |
| title と description が全ページ同じだった | `meta()` を14か所から呼び、ページごとに題・説明文・canonical・OGP・構造化データを出す |

古い `#/` のURLで来た人は、自動で実URLへ送られます（リンクを貼ってくれた人を切らないため）。

## 総点検で見つけて直したもの（2026-09-23）

| 見つけたこと | なぜ問題か | 直したこと |
|---|---|---|
| 存在しないIDのページでエラーが出ていた（`/racer/1` `/venue/99` `/news/nope` `/meeting/…`） | `meta()` が先に動いて null を触っていた。**画面が真っ白** | データが無いときは `meta()` より先に返す |
| 知らないURLがトップページを返していた | 打ち間違いでも200でトップが出る＝**同じ中身のURLが無数にできる**（ソフト404） | 「見つかりません」＋`noindex` |
| レースのタブが別URL扱い | 156レース×10タブ＝**1,560の重複ページ** | canonical を `/race/<id>` に統一 |
| `/d/今日の日付` が `/` と別URL扱い | 重複 | canonical を `/` に |
| `/analysis` と `/analysis/average` が別URL扱い | 重複 | canonical を `/analysis/<種類>` に |
| ニュースのタグ別・選手の検索結果 | 中身が薄い／URLが無数にできる | `noindex` |
| 選手一覧が300人しか出さない | sitemap の1,628人のうち**1,328人がどこからもリンクされない** | `/racers/all`（支部別・全員）を追加 |
| 運営者情報・プライバシーポリシーが無い | GA4でクッキーを使うのに説明が無い。**検索でも信用されにくい** | `/about` `/privacy` を追加、フッターからリンク |
| SNSに貼る画像が無い | X や note に貼ったとき文字だけになり押されにくい | `scripts/make-ogp.mjs` で `site/ogp.png` を作る |
| 日本語の書体を6種類読み込んでいた | 1種類で数百KB。**表示が遅い** | 3種類に減らした |
| 「本文へ移動」が無い・移動しても読み上げ位置が変わらない | キーボードや読み上げの人が使いにくい | スキップリンク・フォーカス移動・`aria-current` |
| `_headers` が実在しないフォルダを指していた | 効いていなかった | 実ファイルに合わせた |
| 日付でないURL（`/d/あいう`）で日付が NaN になる | 壊れた画面が出る | 日付の形でなければ「見つかりません」 |

## 構造化データ

| ページ | 型 | 効き目 |
|---|---|---|
| レース | `SportsEvent`（会場・日時・出場選手6人） | 検索結果にレース情報として出やすくなる |
| 選手 | `Person`（登番・支部） | 「選手名 成績」での表示 |
| ニュース記事 | `NewsArticle`（見出し・日付・発行元） | ニュース枠 |
| その他 | `WebPage` | |

## 設定すること（`site/config.js`）

```js
siteUrl: 'https://（取ったドメイン）',   // ← これが空だと sitemap が作れず、robots.txt が「見に来ないで」のまま
gaId:    'G-XXXXXXXXXX',                // ← GA4 の測定ID
```

**`siteUrl` が空のあいだ、robots.txt はわざと `Disallow: /`（検索に出さない）にしています。**
ドメインが決まる前に中途半端な状態で拾われると、あとで直すのが大変なためです。

## Search Console のつなぎ方

1. ドメインを取る
2. [Search Console](https://search.google.com/search-console) →「プロパティを追加」→ **ドメイン**（URLプレフィックスではなく左側）
3. 出てきた TXT レコードを、ドメインのDNSに追加（Cloudflareなら管理画面のDNS）
4. 確認できたら「サイトマップ」→ `sitemap.xml` を送信

**ドメイン単位（DNS）で確認するのを勧めます。** www あり・なし・http・https を一度に押さえられ、HTMLタグを貼る必要もありません。

## GA4 のつなぎ方

1. [Google アナリティクス](https://analytics.google.com/) でプロパティを作る（データストリーム＝ウェブ）
2. 出てきた測定ID（`G-` で始まる）を `site/config.js` の `gaId` に入れて、サイトを置き直す
3. GA4 →「管理」→「キーイベント」で次の2つを登録する

| イベント名 | 意味 |
|---|---|
| `member_click` | note のメンバーシップへ進んだ＝申し込みに近い行動 |
| `member_unlock` | 合言葉が通った＝会員になった |

ほかに `member_page`（会員ページを開いた）も送っています。
画面の中でページが変わる作りなので、**URLが変わるたびに自分で1ページとして数えています**（GA4の自動計測だと最初の1ページしか数えません）。

⚠ **GA4 は Lcall 専用のGoogleアカウントとは別に作ること**（取り違え防止）。

## 毎回の点検

```
node scripts/seo-audit.mjs          手元の site/ を点検
node scripts/seo-audit.mjs --live   公開中のサイトを実際に取りに行って点検
```

URLの作り・sitemap・robots・ページごとの題・構造化データ・GA4 を見ます。**デプロイ前にこれを通すこと。**

## 数字を見る

Search Console →「検索パフォーマンス」→ 右上「エクスポート」→「CSV をダウンロード」
→ ZIPを展開して中身を `gsc-export/` に入れてから：

```
node scripts/seo-report.mjs
```

出るもの（lcall で実際に効いた見方をそのまま使っています）

1. **あと少しで1ページ目（5〜20位）** … ここに手を入れるのがいちばん効く
2. **表示は多いのに押されない（CTR2%未満）** … 題と説明文の書き換えで伸びる
3. **sitemap に入れているのに一度も検索に出ていないページ** … 中身が薄いか、まだ拾われていない
4. **表示はあるが21位以下** … その語の記事がまだ無い

## まだやっていないこと

**静的HTMLの書き出し（プリレンダ）**。いまは画面をJavaScriptで組み立てているので、Googleは一度JavaScriptを動かしてから中身を読みます。動きはしますが、**1,800ページあると拾われ終わるまでに数ヶ月かかります**。
題・説明文・構造化データだけを先に埋め込んだHTMLをページごとに書き出せば、ここが数週間まで縮みます。ドメインと置き場が決まってから作ります。
