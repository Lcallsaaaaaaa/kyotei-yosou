# 日和データ（1,516項目）の仕様

出典：`POST https://kyoteibiyori.com/request_race_shusso_detail_v4.php`
取り込み：`scripts/biyori.mjs` → `biyori_racer`（JSON）→ `scripts/biyori-flat.mjs` → `bf`（1,518列）

## 検証済みであること

| 検証 | 結果 |
|---|---|
| 登番が番組表と一致 | 3,252 / 3,252 |
| 全国勝率が番組表と一致 | 3,252 / 3,252 |

## 単位（**項目ごとに違う。統一されていない**）

実データとの突合で確定したもの。名前からの推測ではない。

| 項目 | 単位 | 確定の根拠 |
|---|---|---|
| `course{N}_{1,2,3}_ave` | 比率 0〜1 | 0.567 = 56.7%。累積（`_2_ave`は2連対率） |
| `course{N}_nigeritsu` `_sasare` `_makurare` `_makuraresashi` | 比率 0〜1 | 負けた分の内訳構成比。合計1になる |
| `start{N}_ave` | 秒 | 0.17 = ST0.17 |
| `ave_start` | 秒 × 100 | /100した平均0.165 と実測ST平均0.164 が一致 |
| `st_junban_{N}` | 順位そのまま | 3.03位 |
| `ave_start_rank` | 順位 × 100 | 330 = 3.30位 |
| `nami5_rank2` | 2連対率 ‰ | 実測2連対率と r=0.997 |
| `nami5_rank3` | 3連対率 ‰ | 実測3連対率と r=0.998、平均516‰も一致 |
| `nami5_rank1` | **勝率(得点率) × 100** | 実測勝率と r=0.988。**1着率ではない** |
| `zenki/konki_nouryoku_shisuu` | 指数 × 1000 | |
| `tokutenritsu` | 得点そのまま | |

### ⚠️ `nami5_rank1` の件
名前は「rank1」だが**1着率ではない**。2連対率より大きい値が出る（例：450 vs 167）ため
1着率ではありえない。得点率（1着10/2着8/3着6/4着4/5着2/6着1点の平均）×100 だった。

名前から意味を決めていたら、波の高い水面での評価を丸ごと誤っていた。
**すべての項目は実データと突合してから使うこと。**

## 条件の切り口（接尾辞）

| 接尾辞 | 意味 |
|---|---|
| （なし） | 全体 |
| `_nomal` | 一般戦 |
| `_sg` | SG |
| `_woman` | 女子戦 |
| `_tochi` | 当地 |
| `_night` | ナイター |
| `_shonichi` | 初日 |
| `_saishu` | 最終日 |
| `_fmochi` | **F持ち（フライング未消化）状態での成績** |
| `_choku1/2/3/6` | 直近期間 |

## 主要項目

- 事故：`jiko_ten`（事故点） `jiko_ritsu`（事故率）
- F：`mishoka_flying`（未消化F） `zenki_flying`（前期F）
- 決まり手：`kimete_nige/sashi/makuri/makurisashi/nuki/megumare`（全体・SG・一般・女子で別々）
- タイトル：`yusho_count`（優勝） `yushutsu_count`（優出） `junyu_count`（準優） `rank1_count`
- 節内：`tokutenritsu` `konsetsu_start_ave` `konsetsu_display_ave` `junban`
- 前検：`zenken_motor_no/niren/junban` `zenken_boat_no/niren` `zenken_time`
- モーター：`motor_shisuu`（指数） `motor_rank1` `motor_rank_ave` `motor_display_junban_ave`
  `motor_niren_shoritsu` `motor_sanren_shoritsu` `motor_yusho` `motor_yushutsu` `motor_kikan_start/end`
- 日和独自：`nukedashi`（抜け出し） `hekomi`（凹み） `antei`（安定）
- 属性：`seibetsu`（性別） `yousei_kikan`（養成期） `shusshin`（出身）

## 取れないもの

`chiruto`（チルト）は出走表時点では全件NULL。これは直前情報なので
`before_info.tilt`（公式の直前情報）を使う。

## 上位キー（race_list 以外）

- `chukan_seibi_list` 中間整備の履歴（開催キー・モーター番号・交換部品）**公式データには無い**
- `flying_kikan_list` フライング期間
- `maeduke_list` 前づけ
- `kako_list` 過去走
- `start_junban` スタート順番

## ⚠️ 自前データを扱うときの注意

`entries.rank` は **TEXT**（"01"）。数値比較には `rank_num` を使う。
`WHERE rank BETWEEN 1 AND 6` は常に偽になり、結果0件になる。
