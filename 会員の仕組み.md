# 会員（月300円）の仕組み

2026-09-23 に作成。**決済は note のメンバーシップに任せ、サイト側は「合言葉」で開く**形。

## なぜこの形にしたか

決済を自分で受けるには Stripe・Square・国内の決済代行のいずれかで**加盟店審査**を通す必要がある。
本人は過去に審査で止まった・凍結された履歴があるため、この道は個人でも法人でも勧められない
（Stripe は代表者の本人確認情報で名寄せするので、新しく法人を作っても同じ人が代表なら弾かれる可能性が高く、
さらに LCall の Stripe / Square まで巻き込む恐れがある）。

note は**販売者が note 株式会社**で、こちらは出品者にすぎない。加盟店審査が無く、本人確認も特商法表記も note 側で済む。
実際すでに単品200円・月額1000円で売上が立っている＝**確実に動くことが確認できている唯一の道**。

## 流れ

1. お客さんが **note のメンバーシップ（月額300円）** に入る
2. こちらが**その月の合言葉**をメンバー限定の投稿に出す（月1回）
3. お客さんがサイトの「会員」ページで合言葉を入れる
4. その月のあいだ、全レースの**展開予想**と**AI予想（3連複2点プラン）**が開く
5. 解約した人は翌月の合言葉を受け取れない → **解約の反映が自動**

月替わりにしてあるのは、note のメンバーシップが月額だから。日替わりにすると流出には強くなるが、
お客さんが毎日 note を見に行くことになるのでやめた（変えたいときは `seal.mjs` の `periodOf` を直す）。

## 毎月の作業（これだけ）

```
node scripts/paid-key.mjs --note --next
```

出てきた文面を、月末に note のメンバーシップ限定の投稿に貼る。**作業はこれだけ。**
（今月ぶんは `--note`、一覧は引数なしで `node scripts/paid-key.mjs`）

## 中身の守り方

公開の置き場（Supabase の docs 表）は anon の鍵で**誰でも読める**作りのまま。
ログイン（RLS）で守る手もあるが、設定を1か所まちがえた瞬間に全部もれ、しかも気づけない。
そこで **「置き場は誰でも読めるが、中身は合言葉がないと読めない」** 形にした。

- 有料の中身は `paid/race/<レースID>` と `paid/tenkai/<日付>` に **AES-256-GCM の暗号文**で入る
- 鍵は合言葉から PBKDF2（21万回）で作る。**こちらは鍵を預からない**
- 合言葉は master（`data/paid-secret.json`・git に載らない）と年月から決まるので、控えを無くしても何度でも出せる
  - ⚠ **master を失うと過去の合言葉が出せなくなる。控えを取ること**
- 送る前に `putPaid()` で3つ確かめている：①暗号文の形か ②平文の項目名が残っていないか ③開け直せるか

### 2026-09-23 の指摘を受けて直したこと

外部のレビューで挙がった点のうち、すぐ直せるものを直した。

| 指摘 | 直したこと |
|---|---|
| 合言葉が8文字＝約40ビット。暗号文は誰でも取れるので手元で総当たりできる | **12文字＝約60ビット**に延ばした。入力は月1回なので手間は変わらない |
| 鍵を localStorage に文字列で置いていた。よそのJavaScriptが動けば持ち去れる | **取り出せない鍵（extractable:false）にして IndexedDB へ**。復号には使えるが中身は取り出せない。localStorage に残るのは「どの月か」だけ |
| その日の有料データが無いと、まちがった合言葉でも「会員です」と出てしまう | **確認専用の小さな箱 `paid/check/<年月>`** を毎回書き出し、必ずそれで確かめる |
| セキュリティヘッダーに CSP が無い | `site/_headers` に **CSP・HSTS・frame-ancestors・COOP** を追加。手元の確認用サーバーも `_headers` を読むようにして、本番と同じ状態で試せるようにした |
| GA4について「個人を特定できる情報は含まれません」と断定していた | 何を集めるか（端末の識別子・IPアドレスなど）を具体的に書き、断定をやめた |

### 弱点（承知のうえ）

**① 全員が同じ合言葉**。SNSに貼られると、その月は誰でも開ける。会員ごとの停止も端末数の制限もできない。
→ 直すなら「封筒方式」（中身の鍵Kを会員ごとのコードで包んだ小さな箱を人数分置く）。静的サイトのままでき、
　1人だけ無効化できる。**本人の判断待ち**。

**② 暗号文は誰でも取れる**。置き場を公開読み取りにしている設計そのもの。
12文字にしたので現実的な時間では開かないが、「鍵が漏れたら過去ぶんも開く」ことは変わらない
（期間ごとに鍵が違うので、開くのはその月だけ）。

**③ 復号したあとの中身をコピペして配るのは防げない。** これはどんな方式でも同じ。

**④ CSPの style-src に 'unsafe-inline' が要る**（画面の組み立てで style 属性を使っているため）。
スクリプトは 'self' と解析先だけに絞ってあるので、危険度の高いほうは止まっている。

## 無料のままにしたもの

出走表・1着確率・直前情報・オッズ・結果・選手・場の攻略・データ分析・ニュース・**無料枠の単勝1点**。
さらに、**無料枠の単勝1点があるレースは展開予想も無料**にしてある（入口として。9/23は156レース中17レース）。

## 画面の作り

- `/member` … 合言葉を入れる／消す
- 鍵は **IndexedDB（`nagi` / `k` / `member`）に取り出せない形（extractable:false）** で入る。
  `localStorage` の `nagi_member` に残るのは `{ period }` だけで、**合言葉も鍵も文字列としては保存しない**
- 会員でないところには `lockPanel()` の案内が出る
- `site/config.js` の `noteUrl` に note のメンバーシップの URL を入れると、案内に「会員になる」ボタンが出る

## 設定すること（まだ）

- `site/config.js` の `noteUrl` に、note のメンバーシップの URL
- note 側でメンバーシップ（月額300円）のプランを作る
- **note の月額1000円と中身が重なる点**：同じものを300円で出すと1000円の購読者は移る。
  1000円＝note で配信する予想すべて／300円＝サイトの展開予想とAI予想、と分けるか、300円に一本化するかを決める
